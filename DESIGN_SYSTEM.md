# ABRA Design System

> **Как читать этот документ.** Каждый раздел помечен:
> **`[AS-BUILT]`** — реализовано на abra-9bm.pages.dev, это описание работающего кода.
> **`[TARGET]`** — спроектировано, но не построено. Не выдавать за существующее.
>
> Источник истины по as-built значениям — `:root` в `style.css` сайта; их копия лежит
> в `abra-tokens.css` (блок AS-BUILT). Расхождение между сайтом и документом трактуется
> в пользу сайта.

---

## 1. Принципы `[AS-BUILT]`

1. **Кнопка не заливается.** Действие обозначается скобочными уголками и капслоком.
2. **Медь = смысл.** Метка раздела, номер, акцентное слово, состояние. Крупных медных плашек нет.
3. **Один курсив на блок.** Курсивом (Syne italic) выделяется одно слово в заголовке.
4. **Линия вместо тени.** Ни blur, ни свечений, ни теней — только плоскость и 1px граница.
5. **Тело не белым.** Белый — заголовкам и ключевым значениям; абзацы приглушены прозрачностью.
6. **Тон: инженер, не продавец.** Первое лицо, конкретика, срок. Без мотивационной лексики.
7. **Адаптив на clamp().** Фиксированных десктопных отступов нет.

---

## 2. Цвет `[AS-BUILT]`

| Роль | Токен | Значение |
|---|---|---|
| Фон | `--abra-bg` | `#0C0B09` |
| Карточка | `--abra-card` | `#131210` |
| Текст / заголовки | `--abra-text` | `#EDEBE6` |
| Акцент (медь) | `--abra-accent` | `#B87333` |
| Линия | `--abra-line` | `rgba(206,201,195,.07)` |

Всего пять цветовых токенов. Градации текста делаются прозрачностью от `--abra-text`.
Функциональных цветов нет ни одного — даже ошибка формы не подсвечена цветом.

### 2.1 Расширение палитры `[TARGET]`
Градации `--abra-prose / --abra-muted / --abra-faint`, производные `--abra-accent-dim /
--abra-accent-soft` и статусные `ok / warn / bad / info` — предложение для слоёв «контент»
и «кабинет». Вводить только вместе с этими слоями; на лендинге они не нужны.

---

## 3. Типографика `[AS-BUILT]`

Две гарнитуры:

- **Syne** — логотип, заголовки, курсив-акцент. Дисплейный шрифт **без засечек**:
  акцентное слово выделяется медным курсивом Syne, а не серифом.
- **Inter** — тело текста, микро-метки, числа. Отдельной моно-гарнитуры в системе нет:
  «моно-ощущение» меток достигается капслоком и трекингом, а не сменой шрифта.

Правила: заголовки — Syne с плотным трекингом, переносы ставятся вручную; метки — Inter
капслоком с трекингом .18–.28em, 9–11px; тело — Inter, приглушённое прозрачностью.

> Ранняя редакция этого документа предписывала Manrope + Instrument Serif + JetBrains Mono
> и запрещала Inter. Это была ошибка вывода по скриншоту — правило снято, актуальна пара Syne + Inter.
> Макеты-специмены слоёв «контент/кабинет/печать» пока набраны старой парой и подлежат перенабору.

---

## 4. Скобка и иконография

### 4.1 Скобка `[AS-BUILT]`
- **Метка раздела** — `[ .Название ]`, капслок, трекинг, медь.
- **Кнопка** — скобочные уголки слева и справа (см. §5).

### 4.2 Иконки `[AS-BUILT]`
Семь авторских анимированных SVG-иконок, по одной на раздел, с hover- и scroll-reveal-логикой.
Это работающая иконография системы. Сторонние icon-паки и эмодзи не используются.

### 4.3 Уголковая рамка как общий компонент `[TARGET]`
Сейчас достраивающаяся рамка существует только внутри кнопки. Вынесение её в
переиспользуемый паттерн (кадр для изображений, выделение цифры) — отдельная задача.

### 4.4 Юникод-глифы `[TARGET, спорно]`
Набор `[×] ⊞ ≡ ∴ ⊕ (+)` описывался как единственная иконография — это устаревшая идея
из ранней редакции. На сайте иконки уже другие (§4.2). Использовать глифы только в тех
слоях, где SVG-иконок не будет (печать, служебные состояния), и не смешивать с ними.

---

## 5. Кнопки `[AS-BUILT]`

Заливки нет. Скобочные уголки + капслок Inter с трекингом.

- Один размер: высота ≈ 66px.
- **Hover** — уголки достраиваются в полную рамку (фирменная анимация, ядро идентичности кнопки).
- **Loading** — приглушение всей кнопки целиком.
- Заливка, тень, смена цвета фона на hover — запрещены.

`[TARGET]`: размерная шкала 44 / 34 / 26 и точечный loading-индикатор нужны только
интерфейсу кабинета; на сайте не вводить — это ломает анимацию рамки.

---

## 6. Композиция `[AS-BUILT]`

- `--abra-max-w: 1240px`, `--abra-gutter: clamp(20px, 4vw, 56px)`.
- Вертикальные отступы между секциями — через `clamp()`, не фиксированные значения.
- Смысловой блок центрирован, ограничен по ширине измерением текста, а не жёстким px.
- Секция открывается: SVG-иконка раздела → метка `[ .Название ]` → заголовок Syne с одним медным курсивным словом.

`[TARGET]`: фиксированная сетка 1440/88/620 и шаг 8/16/26/40/64/104/160 применимы только
к макетам кабинета и печати, где адаптив не требуется.

---

## 7. Слои

### 7.1 Лендинг `[AS-BUILT]`
Hero с портретом и тёплым градиентом, инсайт-секция с контуром воронки, пять уровней
(Marketing / Sales / Data / Product / System), четыре формата услуг, пять шагов метода,
философия, две формы захвата, футер с правовыми ссылками.

### 7.2 Контент — статьи и кейсы `[TARGET — не построено]`
Лента с чипами-фильтрами и featured-материалом; статья (мета → H1 с курсивом → лид →
автор → тело); элементы: цитата с линией слева, врезка-практика, блок из трёх метрик,
нумерованный список; кейс: цифры на градиенте → разрыв → до/после → таблица «было/стало» → отзыв.
Логотипы клиентов не публикуются: отрасль и размер компании.

### 7.3 Продукт — личный кабинет `[TARGET — не построено]`
Сайдбар с разделами и прогрессом этапа; дашборд: четыре метрики со спарклайнами,
воронка с отметкой сверхнормативной потери, список разрывов (критично / внимание / наблюдение),
таблица шагов со статус-бейджами; состояния: скелетон вместо спиннера, честное пустое состояние.
Именно здесь впервые появляются функциональные цвета и плотная сетка.

### 7.4 Печать `[TARGET — не построено]`
Визитка 90×50 (лицо тёмное с медным тиснением, оборот светлый), бланк A4 (светлый, поля 20мм,
тело 11–12pt), обложка отчёта и слайды 16:9 (тёмные, с градиентом), бейдж 70×100.
Медь — Pantone 7568 C или фольга; чёрный CMYK 60/50/50/100; вылеты 3мм.
Тело на тёмном фоне не печатается.

---

## 8. Запрещено `[AS-BUILT]`

Заливка кнопок; медные плашки крупнее кнопки; тени, blur, свечения; сторонние иконочные
наборы и эмодзи; больше одного курсива на блок; больше одного градиента на страницу;
белый текст в абзацах; растянутый или обведённый логотип; фиолетово-синие «AI-градиенты»;
подмена Syne или Inter другой гарнитурой.

---

## 9. Открытые решения

1. **Перенабор макетов слоёв контент/кабинет/печать** со Manrope + Instrument Serif + JetBrains Mono на Syne + Inter.
2. **Судьба юникод-глифов** (§4.4): оставить как служебный набор для печати или выпилить.
3. **Статусные цвета** — вводить сразу пятёркой или начать с трёх (ok / warn / bad).
4. **Ошибка формы на сайте** сейчас без цвета — назначить ей `--abra-bad` или оставить текстом.
